# Project preferences

- This app makes realistic tree modeling approachable while keeping generation and rendering lightweight.
- Use the native Rust backend and TypeScript / Three.js frontend. Do not introduce Docker or a bundled CPU Ollama service. Connect to the user's existing Ollama.
- Keep setup and startup commands at the repository root. Bind to localhost by default.
- The user delegates design and feature choices; prefer a professional, clear production-tool interface.

# Continuity

The user explicitly requested on 2026-09-07 that relevant past and future conversations be remembered in Obsidian. For this project, use the obsidian-memory skill to read and maintain `C:/Users/yuki_/Documents/Obsidian Vault/CodexMemory/Projects/l-system.md` when durable decisions or verified project state change. Preserve unrelated content, never save secrets, and distinguish unfinished work from verified outcomes. Save useful context rather than full transcripts. Current user instructions take precedence over remembered preferences.
